# react-hydrate-uid-mismatch-sample

This is a demo of the behavior when setting a unique ID for the Label attribute in React's SSR.

## install

```
$ yarn
```

legacy mode
```
$ yarn dev
```

concurrent mode
```
$ yarn dev:concurrentmode
```